// This is an empty layout file. It can be removed if not needed.
// Or used to wrap admin-specific layout components.
export default function AdminLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
